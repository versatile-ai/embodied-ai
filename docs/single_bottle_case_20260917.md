# 单瓶入桶基础用例：通过

日期：2026-09-17。会话：`e7c591c27915`。运行位置：Windows 本地 MuJoCo / Intel Arc 渲染。布局：`put_bottles_into_dustbin_0.json`；观察视角 `overview_yaw=-45`，便于看到垃圾桶。

## 执行与证据

使用左臂抓取左侧白色瓶子 `bottle3`。通过现有 HTTP 动作/末端接口执行固定布局路径点，不调用 π0.5，不直接设置瓶子坐标，也不改评分逻辑。

| 阶段 | 控制步 | 结果 |
|---|---:|---|
| 打开夹爪并接近瓶子上方 | 0–56 | 到达约 (-0.225,-0.079,1.101) |
| 下降到瓶身附近 | 56–77 | 到达约 (-0.224,-0.079,0.906) |
| 闭合夹爪 | 77–92 | 第 85 步首次记录抓住 bottle3 |
| 抬升并搬运到桶口上方 | 92–216 | 夹持保持；到达约 (-0.624,-0.095,1.051) |
| 松爪并等待重力落入桶内 | 216–246 | 第 237 步首次入桶，计数 1，得分 10 |
| 追加保持检查 | 246–256 | 仍入桶 1，左右抓取状态均 None |

最终断言通过：`bottles_in == 1`、`score == 10`、`grasped.left is None`。保存 257 条状态及三路 MP4，所有视频全量解码通过。原始请求、响应和逐帧图像位于 `simulation/mujoco/runs/e7c591c27915/`。

## 边界

这是固定布局路径点驱动的功能验证，不是学习策略成绩。使用环境原有 `GRASP_ASSIST`：两指接触同一瓶子后维持夹持位置，开爪解除；并非严格的无辅助摩擦抓取。

单瓶用例完成后主动结束会话。完整任务要求四个瓶子入桶并回位，因此服务的 `success` 仍为 false、终止原因为 `stopped_by_operator`，不能宣称完整四瓶任务成功。
