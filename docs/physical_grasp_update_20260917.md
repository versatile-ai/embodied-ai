# 2026-09-17 停止实验与物理抓取版本更新

- 用户要求停止 GPT6+π0.5 三局实验，复位并合入远程最新提交。
- 使用 STOP 文件在下一动作提交前安全停止控制循环，runner 已退出。正式第1局停在117步，0瓶入桶；第2、3局未启动。这是用户中断，不计为已完成策略失败。
- 已调用旧服务 finish，保留 `runs/02810e23fd90/` 三路 MP4、逐帧状态与实验目录 `runs/gpt6_hybrid_20260917_192037/` 的完整决策记录。HTML 标记 stopped_by_user。
- 工作分支 feature/RoboDojo 从14a4c88快进至dd63456，包含05dd877、cc0fc72、dd63456。本地修改先保存至stash，恢复后保留Windows视频、单次大场景编译、资源释放、静态桶/支架等兼容性修改；备份未删除。
- 冲突解决：瓶子计数保留布局瓶子标签集合，健康端点保留v5与本地render_backend字段。
- GPT6提示输入递归过滤新字段object_positions，包括历史执行结果；原始观测/日志不删除。HTML抓取辅助说明改为读取实际sim_health，历史实验仍显示true。
- 新服务版本astra-isolated-v5-physical-grasp，默认grasp_assist=false；新会话b30743a19b0d使用布局0，停在t=0，未提交任何动作。reset_check.json全部8项通过。
- 首轮27项回归测试26通过、1失败：右臂物理搬运双指接触率0.9757575757575757，低于0.98阈值。左右目标瓶均完成入桶，左臂稳定性与新会话复位测试通过。没有调低阈值或修改物理参数掩盖失败。
- 新版不应视为物理稳定性全项验收通过；正式实验保持停止。
