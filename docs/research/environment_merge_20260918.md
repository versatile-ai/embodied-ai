# 实验环境优化提交与远程合并

本地优化提交：`00d94f4`。合并远程 `feature/RoboDojo` 至 `32ca80a`，保留远程新增的评测入口、I/O 审计和连续夹爪控制。

冲突处理保留了双方意图：刚体运行时质量上限、初始张开、策略夹爪命令状态、关节轨迹限速与 EEF 慢限速，同时保留远程 measured-anchor 控制。连续夹爪测试按初始全开的约定检查开度下降。

## 本次检查

- 合并前：33 项本地相关测试和 2 项 NPU 策略契约测试通过。
- 合并后：综合运行 55 项，53 项通过、2 项错误。
  - 分类场景在模型编译时内存分配失败；单独重跑同样失败，尚未确认是资源上限还是场景内存需求问题。
  - 远程新增 `test_eval_control` 无法在原生 Windows 导入，原因是 `fcntl` 为 Linux/Unix 平台依赖；未把它标记为通过。
- 合并后的跟踪回归测得最大关节误差约 0.0193 rad，目标滤波滞后约 0.00706 rad。
- Python 合并审查与 Git 空白/冲突检查通过。

## 限制

远程新增 ARM_KP=2400、ARM_KD_FLOOR=8，改变了动力学；先前 KP=1000 下的抓取成绩不能视作合并版本验收。尚未运行合并版本完整抓取实验。提交代码并不等于重启本机仿真或部署 NPU 服务。

xhigh 首次审核超时仍未解决，上一局停在预检第 0 步。原先左臂严格双边接触比例测试仍有未解决记录。本次提交不宣称整体实验已成功。

运行日志、录像、模型权重和本地认证信息不包含在优化提交中。
