# 已知问题修复与验收（2026-09-18）

## 修复范围

- NPU π0.5：显式设置 `adapt_to_pi=False`、`use_delta_joint_actions=True`，按 RoboDojo ARX X5 seed0 的训练配置解释输入和输出。没有更换、重新训练或重新转换权重。
- MuJoCo：取消夹爪 0.2/0.8 滞回开关，恢复连续归一化命令；初始左右双指均张开到 0.044 m。
- 策略状态：手臂仍为实测关节角，夹爪改为上次命令开度。另存 `measured_state` 供诊断，不能把命令闭合当成抓取成功。GPT 提示与报告已同步。
- 跟踪：关节轨迹每步目标限幅最终改为 0.15 rad；保留 EEF 控制原有 0.028 限幅以及真实接触、夹爪限幅和控制平滑。初步 0.05 对模型约 2.5 rad/s 的轨迹仍不足，见下文。未启用抓取吸附。
- GPT 审核：所有模式都校验非空执行/意图证据；`not_started` 仅允许且要求出现在第 0 步。edit 平移及旋转偏移随前缀按 `(i+1)/steps` 渐进，夹爪覆盖立即生效。

## 自动化验证

- NPU 工厂配置：2 项通过；故障参数先产生红测试，修复后转绿。
- 夹爪协议、关节跟踪、左右臂真实接触抓取：最终 9 项通过（30.42 秒，包含新增 2.5 rad/s 双臂轨迹）。左右臂各自完成抓取、搬运、投入垃圾桶；这是预设控制验收，不是 π0.5 自主完成。
- 跟踪测试最大误差 0.08174 rad，目标限幅滞后 0.00706 rad。
- 左/右夹爪最大同步误差 0.1738 / 0.1747 mm；搬运双侧接触比例均 0.9818。
- 服务测试 15 项中 14 项首次通过；分类场景首次遇到 `Could not allocate memory`，释放运行中仿真资源后独立重试通过。不能声称首次整套无错误。
- 审核协议与 runner：13 项通过；新增 3 项先复现缺陷，再验证修复。
- Windows 工具及日志审计：7 项通过。
- Python 代码审查通过，包括命令/实测开度的下游语义检查。

## 部署

- 跳板机 `121.91.172.53`，推理服务器 `192.168.0.192`。
- 原服务备份：`/data/pi05_hybrid/scripts/pi05_service.pre-arx-v2-20260918.py`。
- 原服务 SHA256：`3bb56e78180918d87675a689cf05a771444cd0ee566f3996144ebbcf16854673`。
- 新服务本地与远端源码 SHA256 一致：`b7ca67abcef6e3b82b6bf1d823a0363c8e5f40f302e73953c6e7a75eaea31eab`。加载后仅同步了说明文档字符串的修正，无功能更改。
- 新 NPU 健康标识：`robodojo-arx-x5-v2`；本地仿真：`astra-isolated-v6-arx-contract`。
- 在已安装 openpi 环境实际验证 `AlohaInputs/Outputs(adapt_to_pi=False)` 保持关节角和夹爪归一化值不变；配置测试不是唯一依据。
- 推理服务仅面向单实验调用方；没有验证并发调用共享模型的安全性。

## 验证边界

首个短程 π0.5 闭环会话 `e8c1bfd27f75` 在 20 步被诊断阈值停止：0.05 rad 限速下，跟踪误差 0.55874 rad，其中目标滤波滞后 0.48033 rad。没有抓起瓶子。三路录像和所有状态、预测均保留。

保持真实接触、同一段动作的离线回放：

| 每步目标限幅 | 整段最大误差 | 第20步误差 | 第20步滤波滞后 |
| --- | --- | --- | --- |
| 0.05 rad | 0.55874 | 0.55874 | 0.48033 |
| 0.10 rad | 0.30526 | 0.21511 | 0.05996 |
| 0.15 rad | 0.23066 | 0.12992 | 0.00941 |

采用 0.15 后重新执行短程验证，会话 `80e6faab9826`：20 次新预测、每次执行前 10 步，累计 200 步后主动结束。模型原始采样未固定；GPT 没有参与此轮诊断。所有帧的最大关节误差 0.22152 rad，平均逐帧最大关节误差 0.07925 rad；预测夹爪最小值 0.05546，原先错误映射造成的半开下界已消失。

**任务结果未通过：没有稳定抓起瓶子，也没有投进垃圾桶。** 第 40 步只有一次接触抓取标签，bottle3 最大增高仅 3.28 mm，之后被碰倒、推动；不能算抬起。已保存全部预测、每步状态和三路 MP4 于 `simulation/mujoco/runs/80e6faab9826/`。服务保持在线，实验已停止。

此轮不启动 3 局正式 GPT+π0.5 实验。限速修改不是碰撞安全保证，且两次在线模型采样不同，不能把两次在线结果当严格配对消融；上表使用同一保存轨迹才是配对比较。

下一步应对齐真实相机图像/坐标、抓取接近姿态和接触参数，并以相同观测固定采样对照 NPU 与作者推理输出。当前证据不足以归因于权重本身失效，也不足以证明复现环境已能复现作者的任务成功率。不应再盲目扩大增益或关闭接触来制造成功。

仍不能宣称与作者实验完全等价：MuJoCo 与 Isaac Sim、NPU/PyTorch 与原 JAX、图像外观、IK 和接触动力学均有差异。修复接口错误不等于已经证明模型任务成功率。
